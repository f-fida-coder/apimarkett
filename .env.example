# MongoDB connection for WhatsApp lead form submissions
# Replace <db_password> with your real password and keep URI quoted.
MONGODB_URI="mongodb+srv://numericfida786_db_user:<db_password>@cluster0.unnpc7g.mongodb.net/?appName=Cluster0"

# Optional database name (defaults to "apiflow")
MONGODB_DB_NAME="apiflow"
